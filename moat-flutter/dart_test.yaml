tags:
  integration:
    # Skipped by default — requires external services (e.g. Drawbridge relay).
    # Run with: ./scripts/test-drawbridge-integration.sh
    skip: "Requires running Drawbridge relay server"
