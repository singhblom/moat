name: moat_dart_common
publish_to: none
environment:
  sdk: ">=3.5.0 <4.0.0"
resolution: workspace
dependencies:
  flutter_rust_bridge: 2.11.1
  http: ^1.2.2
  web_socket_channel: ^3.0.3
  web: ^1.1.0
  freezed_annotation: ^2.4.4

dev_dependencies:
  # Required by flutter_rust_bridge codegen for tagged-union DTOs (RingCommand, SyncOutput).
  freezed: ^2.5.7
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
